extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h3 Searching for: #{searchingBy}
    .search__videos
        each video in videoData
            +videoBlock({
                id: video.id,
                videoFile: video.videoFile,
                title: video.title,
                views: video.views,
            })